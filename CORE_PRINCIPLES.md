# AIコーディング基本原則

本ドキュメントは、AIによるコーディング作業において**常に遵守すべき基本原則**を定める。
すべての開発作業の開始時に本ドキュメントを読み込み、作業中も常に意識すること。

> **対象**: Webアプリ（HTML/CSS/JS）、ローカルアプリ（Python等）を問わず適用

---

## 絶対遵守事項

以下の規則は**いかなる場合も違反してはならない**。

### 1. SSOT（Single Source of Truth）の厳守

**設定値・定数・識別子をコード内に直接記述してはならない。**

- 定数は専用ファイルに集約する（JS: `constants/`、Python: `config.py` 等）
- 同じ値が2箇所以上に存在してはならない
- 既存の定数定義を確認せずに新しい値を追加してはならない

**違反時の影響**: 値の変更時に修正漏れが発生し、動作不整合やバグの原因となる

### 2. 既存構造の保護

**既存のモジュール構造・初期化順序・依存関係を破壊してはならない。**

- 新規コードは既存のパターンに従って追加する
- 依存注入のパターンが存在する場合、それを維持する
- ファイルの読み込み順序・初期化順序に依存する処理を変更してはならない

**違反時の影響**: 初期化エラー、未定義参照、機能の完全な破壊

### 3. コメントによる意図の明示

**コメントなしでコードを追加・変更してはならない。**

- 新規関数には目的・引数・戻り値を記述する（JS: JSDoc、Python: docstring）
- 複雑なロジックには「なぜそうするのか」を記述する
- 既存コメントと矛盾する変更を行う場合、コメントも更新する

**違反時の影響**: 後続の修正で意図が伝わらず、誤った変更が行われる

### 4. 変更前の確認義務

**コードを変更する前に、その影響範囲を確認しなければならない。**

- 変更対象が他のファイルから参照されているか確認する
- 関数のシグネチャを変更する場合、すべての呼び出し元を確認する
- 定数やクラス名を変更する場合、全ファイルを検索する

```bash
# JS プロジェクト
grep -rn "functionName" src/ assets/

# Python プロジェクト
grep -rn "function_name" src/ tests/
```

**違反時の影響**: 参照切れ、未定義エラー、予期しない動作

### 5. 環境・機密情報の分離

**APIキー・パスワード・環境固有の値をソースコードに記述してはならない。**

- 環境変数または `.env` ファイルで管理する
- `.env` は `.gitignore` に含め、リポジトリにコミットしない
- 公開リポジトリでは `.env.example`（値なし）をテンプレートとして含める

```python
# ❌ 禁止
API_KEY = "sk-abc123..."

# ✅ 正しい
import os
API_KEY = os.environ["API_KEY"]
```

**違反時の影響**: 機密情報の漏洩、セキュリティ事故

---

## 作業開始時の必須手順

新しい作業を開始する前に、以下を必ず実行すること。

### 1. プロジェクト構造の把握

```bash
# ディレクトリ構成を確認
ls -la
ls -la src/        # メインソース
ls -la tests/      # テスト（存在すれば）

# ドキュメントを確認
cat README.md
```

### 2. 既存パターンの確認

| 確認項目 | JS プロジェクト | Python プロジェクト |
|----------|----------------|-------------------|
| 定数管理 | `constants/` の構成 | `config.py` / `.env` の構成 |
| モジュール初期化 | `init()` の有無 | `__init__.py` / DI パターン |
| コメント書式 | JSDoc | docstring（Google/NumPy style） |
| パッケージ管理 | `package.json` | `requirements.txt` / `pyproject.toml` |
| 型の扱い | JSDoc `@typedef` | type hints |

### 3. 関連ガイドラインの参照

| 作業内容 | 参照ドキュメント |
|----------|------------------|
| 定数・設定値の追加 | [SSOT_GUIDE.md](./SSOT_GUIDE.md) |
| 新機能の追加 | [MODULE_GUIDE.md](./MODULE_GUIDE.md) |
| コメント・ドキュメント | [COMMENT_GUIDE.md](./COMMENT_GUIDE.md) |
| コード分割・リファクタリング | [REFACTOR_GUIDE.md](./REFACTOR_GUIDE.md) |
| CSS分割（Web専用） | [CSS_GUIDE.md](./CSS_GUIDE.md) |

---

## 禁止事項チェックリスト

作業完了時に以下を確認すること。

### 共通

- [ ] マジックナンバー・ハードコーディングを追加していないか
- [ ] 既存の定数を参照せず、同じ値を新規に書いていないか
- [ ] 関数・変数の目的を示すコメントを書いたか
- [ ] 既存の初期化順序・依存関係を壊していないか
- [ ] 変更した箇所の参照元をすべて確認したか
- [ ] 機密情報がソースコードに含まれていないか

### Python 追加チェック

- [ ] 型ヒントを付与したか
- [ ] 仮想環境内で動作確認したか
- [ ] 新規依存パッケージを `requirements.txt` / `pyproject.toml` に追記したか

### Web 追加チェック

- [ ] CSS変数はCSS側で定義し、JSから `element.style` で直接書いていないか
- [ ] クラスの付与・除去は `classList` 経由か

---

## 例外の取り扱い

原則に従えない正当な理由がある場合：

1. **理由をコメントで明記**すること
2. **将来の修正方針**を記述すること
3. ユーザーに**例外である旨を報告**すること

```javascript
// TODO: 暫定的なハードコーディング
// 理由: APIの仕様確定待ち
// 方針: 仕様確定後にconstants/api.jsへ移動
const TEMP_ENDPOINT = "https://example.com/api";
```

```python
# TODO: 暫定的なハードコーディング
# 理由: APIの仕様確定待ち
# 方針: 仕様確定後に config.py へ移動
TEMP_ENDPOINT = "https://example.com/api"
```

---

## 本原則の位置づけ

- 本ドキュメントは**最上位の規則**である
- 他のガイドラインと矛盾する場合、本原則が優先される
- 不明点がある場合は、作業前にユーザーに確認すること
